---
name: publish
description: "Publish a Nostr text note (kind 1/9) to a running hush-relay using raw line protocol or external tool."
---
# publish (Hush)

Hush relay speaks newline JSON arrays over TCP (MVP).
For real Nostr use a client pointed at ws://... (future adapter) or wrap with a small bridge.

Quick test publish (from relay skill).
